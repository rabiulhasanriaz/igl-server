name: Sync GitHub to GitLab

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check GitLab configuration
        env:
          GITLAB_TOKEN: ${{ secrets.IGL_SMS }}
          GITLAB_USERNAME: ${{ secrets.IGL_SMS_USERNAME }}
          GITLAB_REPOSITORY: ${{ secrets.IGL_SMS_REPO }}
        run: |
          test -n "$GITLAB_TOKEN" || { echo "IGL_SMS is missing"; exit 1; }
          test -n "$GITLAB_USERNAME" || { echo "IGL_SMS_USERNAME is missing"; exit 1; }
          test -n "$GITLAB_REPOSITORY" || { echo "IGL_SMS_REPO is missing"; exit 1; }

          echo "GitLab configuration found."
          echo "Repository: $GITLAB_REPOSITORY"

      - name: Sync to GitLab
        env:
          GITLAB_TOKEN: ${{ secrets.IGL_SMS }}
          GITLAB_USERNAME: ${{ secrets.IGL_SMS_USERNAME }}
          GITLAB_REPOSITORY: ${{ secrets.IGL_SMS_REPO }}
        run: |
          git remote add gitlab \
            "https://${GITLAB_USERNAME}:${GITLAB_TOKEN}@gitlab.com/${GITLAB_REPOSITORY}.git"

          git push gitlab main